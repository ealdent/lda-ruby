#!/usr/bin/env bash
set -euo pipefail

IMAGE_NAME="${IMAGE_NAME:-lda-ruby-dev}"

docker build -t "${IMAGE_NAME}" .
docker run --rm -v "$(pwd)":/workspace -w /workspace "${IMAGE_NAME}" bundle exec rake test
